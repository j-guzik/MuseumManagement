@model IEnumerable<MuseumManagement.Models.City>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CityName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CityName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCity">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCity">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCity">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
