@model IEnumerable<MuseumManagement.Models.ItemAuthor>

@{
    ViewData["Title"] = "Index";
}

<h1>Twórcy i ich dzieła</h1>

<p>
    <a asp-action="Create">Dodaj</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Item)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Author)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Item.ItemName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Author.AuthorName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdItemAuthor">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdItemAuthor">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdItemAuthor">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
